"""basecli — a headless renderer for Obsidian Bases (.base) files.

Obsidian has no headless renderer for Bases; the engine only runs inside the
GUI app. This package reimplements that engine so AI agents (and humans) can
evaluate a ``.base`` view against a vault of Markdown notes from the command
line. See README.md for the output contract and LIMITATIONS.md for scope.
"""

__version__ = "0.1.0"
